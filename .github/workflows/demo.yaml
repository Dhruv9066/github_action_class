name: my_workflow
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
      - name: Sleeping 
        run: sleep 30
        
      - name: echoing
        run: echo "Hello from dhruv" && 
